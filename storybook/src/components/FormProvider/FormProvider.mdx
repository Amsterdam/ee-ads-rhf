import { Canvas, Controls, Meta, Primary } from '@storybook/addon-docs/blocks';
import * as FormProvider from './FormProvider.stories';

<Meta of={FormProvider} />

# FormProvider

A component to provide [React Hook Form](https://react-hook-form.com/) Context and allow consuming component to subscribe to context and use [useForm](https://react-hook-form.com/docs/useform) props and methods.

## References

- [React Hook Form - FormProvider Docs](https://react-hook-form.com/docs/formprovider)

<Primary />

<Controls />
